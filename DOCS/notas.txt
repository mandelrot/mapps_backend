- Las carpetas de apps no pueden llamarse "admin" ni "main".
- Dentro de "backend/database se creará una carpeta "files", donde (si toca) se colocarán los ficheros subidos.

- Explicar por qué NO SE PUEDE CAMBIAR el nombre de la carpeta: carpeta = app id

Dónde cambiar el puerto:
  - config
  - backend main
  - frontend apps socket

LAS RUTAS NO PUEDEN TENER DOS PUNTOS SEGUIDOS